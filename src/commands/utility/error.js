const { SlashCommandBuilder } = require('discord.js');

module.exports = {
	owner: true,
	data: new SlashCommandBuilder()
		.setName('error')
		.setDescription('Creates an error'),
	async execute() {
		a;
	},
};